.row
  .ninecol
    .org-show
      .row
        .tencol 
          .ninecol
            .eightcol
              .show-item
                .row
                  .fivecol 
                    Move Student
                    .row
                      .onecol
                        &nbsp;
                      .eightcol
                        %form{:method => "post", :action => "/request_update/#{@request.id}", :class => "request_form"}
                          #{@request.name} &nbsp;
                          Project ID: 
                          %input{:name => "project_id", :type => "text", :value => "#{@request.project.id}"}/
                          %input{:type => "submit", :class => "submit-button", :value => "Update"}/ 
                    


        
  .twocol
    .org-panel-box
      %p{:class => "recent-item-header"} Recent Projects
      -@recent_projects.each do |project|
        .row
          .recent-item
            %a{:href => ("/project/#{project.id}")} #{project.name} 